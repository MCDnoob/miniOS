# 输入输出

CPU外部设备寄存器的编号就是端口号，一般最大值为65536

CRT：是 VGA（视频图形阵列）显卡中的一组控制寄存器，负责管理显示器的时序、分辨率、扫描方式等核心显示参数，是早期显卡（如 VGA、EGA）实现图形 / 文本显示的关键硬件组件。CRT共有24个寄存器，现在主要用这几个：
- CRT 地址寄存器 0x3D4
- CRT 数据寄存器 0x3D5
- CRT 光标位置 高位 0xE
- CRT 光标位置 低位 0xF

端口：外部设备的内部寄存器，一般连接在南桥芯片上