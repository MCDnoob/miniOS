# 可变参数原理

```C++
int printf(const char* format, ...);
```

- `va_list`：保存可变参数指针
- `va_start`：启用可变参数
- `va_arg`：获取下一个参数
- `va_end`：结束可变参数

一般来说，可变参数原理第一个参数要标记参数的数量，`printf`也不例外，但是它是传入一个指针，里面的`%`揭示了参数的数量`format`。